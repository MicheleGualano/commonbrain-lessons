# OS / editor
.DS_Store
*.swp

# local scratch + managed clones used by `cbrain publish`
/tmp/
*.tmp
.cbrain-clone/

# derived build artifacts (regenerated by CI from lessons/)
/build/
lessons.jsonl

# secrets — never commit
.env
*.local
service-role*.key
